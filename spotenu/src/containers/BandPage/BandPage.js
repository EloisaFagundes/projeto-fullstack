import React, { Component } from "react"

// Abanda pode criar álbum(passar nome e selecionar genêros), 
// músicas(Aqui é o onde as bandas criam músicas Para isso, devem fornecer o nome e o selecionar um álbum que já tenham criado.)

class BandPage extends Component {

    render() {
        return (
            <div>
                <p>band page</p>
            </div>
        )
    }
}
export default BandPage