import React, {Component} from "react"


// vai receber a lista de bandas para aprovação(lista com todas as bandas e um botão que permita aprovar cada uma delas), 
// cadastrar gêneros(informando só o nome).

class AdminPage extends Component {

    render() {
        return(
            <div>
                <p>admin page</p>
            </div>
        )
    }
}
export default AdminPage